# Portfolio Frontend

React + TypeScript + Vite frontend for Ecatu Ronald's professional portfolio.

## Commands

```bash
npm install
npm run dev
npm run lint
npm run build
npm run preview
```

Public content is managed in `src/data/portfolio.ts`.
Optimised photography and the social-sharing image are stored in `public/images/`.
