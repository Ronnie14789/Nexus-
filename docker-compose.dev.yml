services:
  mongodb:
    image: mongo:7.0
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: ecatu_portfolio
    volumes:
      - mongo_dev_data:/data/db

volumes:
  mongo_dev_data:
